
export const PROVIDERS = Object.freeze({
  FACEBOOK: 'FACEBOOK',
  INSTAGRAM: 'INSTAGRAM',
  WHATSAPP: 'WHATSAPP',
});

export const ALLOWED_PROVIDERS = Object.freeze(Object.values(PROVIDERS));

export default PROVIDERS;
